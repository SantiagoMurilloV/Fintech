{
  "name": "mandioca-front",
  "version": "0.4.0",
  "private": true,
  "description": "Mandioca — PWA del panel financiero. Sin bundler ni dependencias: módulos ES nativos.",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 3000",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
